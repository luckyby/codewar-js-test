// var arr = ["Яблоко", "Апельсин", "Груша"];
let arr = ["a", "b", "c", "a", "d"];
let new_arr = [];
arr.forEach(function (item, i, arr) {
  if (item == arr.forEach(function (elem, j, array) {

      return
    })) console.log('i:' + i + 'item:' + item);
  // console.log("item:" + item + "; " + "i:" + i + "; " + "(массив:" + arr + ")");
});